# M1-T01｜建立 `/project` 与 Python 工程骨架

状态：已完成

完成日期：2026-08-09

## 完成内容

- 建立 `app/`、`tests/` 与 `pyproject.toml` 工程骨架。
- 配置可编辑安装及 `pytest` 开发依赖。
- 新增 `app/info.py`，以 `APP_VERSION` 暴露应用版本。
- 更新 `app/main.py`，启动时输出应用名称和版本。
- 扩展冒烟测试，验证应用名称及版本常量。

## 验证结果

在项目虚拟环境中执行：

```bash
.venv/bin/python -m pytest
.venv/bin/python -m app.main
```

结果：

- pytest：`2 passed`
- 应用入口输出：`AI Workspace Lite started. 0.1.0`

## 遗留与下一步

- 当前仅有工程骨架与冒烟测试；M1 后续任务将逐步引入数据模型、业务逻辑、JSON 持久化与更多测试。
- 下一任务：M1-T02｜Python 对象引用与可变对象。
