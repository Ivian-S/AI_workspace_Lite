# 新增：/project/pyproject.toml

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-workspace-lite"
version = "0.1.0"
description = "A lightweight AI workspace backend learning project."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
]

